export default function (num) {
    function teste2(n) {
        var contador = n
        while (contador > 0) {
            console.log ("hidrocor");
            contador = contador - 1
        }

    teste2(num)
    } 
}